# Full Machine Assembly And Next Interfaces

## Status

Current working assembly hypothesis.

## Current assembled reading

Canonical stack:

- `L1 = CHAOS`
- `L2 = FORMATION`
- `L3 = MARKET`
- `L4 = DEMAND`

Current machine reading:

- `L1-L3` are the machine core
- `L4` is not the core
- `L4` is manifestation, demand, I/O, and upper cycle closure

## Core cycle

```text
L4 substrate -> L1 chaos -> L2 formation -> L3 market -> L4 demand/manifest -> residue -> L1
```

Meaning:

- `L4` provides external substrate and user-facing manifestation
- `L1` chaotizes and drives emergence
- `L2` forms distinguishable internal form
- `L3` lets forms live, compete, persist, and become live adapter state
- `L4` manifests one narrow actualization outward
- residue returns into `L1`

## What is already strong

- `L1` exists as a real body
- lower `L2` exists as a real body
- `ENCODE` is no longer a black box
- `L3` exists as a useful temporary life-space
- `L4` is now readable as a frozen, replaceable upper layer

## Current interpretation of `L4`

`L4` may be:

- a local transformer
- a remote API model
- another ready-made manifestation substrate

The important rule is:

- `L4` base weights remain frozen during normal machine work

`L3` should adapt or condition `L4`, not retrain it fully.

## Three main unresolved interfaces

### 1. `L4 -> L1`

This is now the most important unresolved interface.

Question:

- what exactly from `L4` enters `L1`?

Not "the whole transformer".

Possible forms:

- text slices
- token streams
- embeddings
- noisy fragments
- active windows from frozen substrate
- perturbation packets

### 2. `L2 -> L3`

Partially alive already, but still temporary.

Question:

- what exactly leaves `FORMATION` and enters `MARKET`?

Candidates:

- single trigram
- short trigram phrase
- crystal phrase
- compact calm object

### 3. `L3 -> L4`

The upper adaptation bridge.

Question:

- how does `MARKET` condition `DEMAND`?

Possible forms:

- soft prompt
- prefix state
- KV-like memory
- adapter state
- latent routing bias

## Priority

The current priority is:

```text
L4 -> L1
```

Reason:

- without this, the new full-machine hypothesis cannot be tested honestly
- this is the first place where the external substrate actually touches the machine core
- if this interface is wrong, the whole upper-half architecture remains speculative

## Immediate next move

Do not jump to the whole machine at once.

Do this first:

1. define `L4` substrate type
2. define what unit from `L4` enters `L1`
3. define first `L4 -> L1` feed mechanism
4. only then continue upward integration

## Short conclusion

The machine is now assembled enough to stop blind exploration.

The next step is no longer "general architecture".

The next step is:

```text
formalize the L4 -> L1 interface
```
