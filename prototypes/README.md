# prototypes

This directory contains the old zig packet prototype.

It is kept here because the neural branch is not detached from packet.
We are not inventing a new thing from nothing.
We are transferring packet logic onto neural-compute rails.

Current role of the zig prototype:

- ontological anchor
- source of layer semantics
- comparison body for future L2 and above
